Dai, provaci anche tu!!! :D

In HTML, CSS e Bootstrap (NO JavaScript) ricrea la pagina Home per un sito di annunci chiamato Presto.it (deve essere responsive quindi parti dalla versione mobile!!!)

Qui trovi le risorse necessarie quali:
    in /public/css/style.css trovi le variabili CSS per la definizione dei colori del tema
    in /public/img le immagini

Ti ricordo che quando crei un nuovo progetto devi:
    Creare una directory del progetto per esempio Presto
    Inizializzare una repository git locale e una remota
    Creare almeno un file index.html immediatamente all'interno della cartella del progetto
    Effettuare un commit per ogni step importante effettuato (ho fatto la navbar -> commit, ho aggiunto il carosello -> commit, ho modificato il carosello perchè ho scoperto un bug -> commit, ...)